const api = 'http://localhost:3030';

const user = api + 'user';
const session = api + 'session';
const activity = api + 'activity';

const url =  {
  user,
  session,
  activity
};

export default url;
