import React from 'react';

const Loading = () => {
    //console.log(props.state)
    return (
        <div className="flex" id="loading">
            <img id="imgLoading" src="/loading-bt.gif" alt="loading..." />
        </div>
    );
}

export default Loading;